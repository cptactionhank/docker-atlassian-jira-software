sudo: required

language: ruby

services:
  - docker

before_install:
  - docker version
  - docker info
  - docker pull postgres:9.3
  - docker pull mysql:5.6
  - docker pull java:8

before_script: bin/prepare

deploy:
  provider: script
  script: bin/deploy
  on:
    branch: master
